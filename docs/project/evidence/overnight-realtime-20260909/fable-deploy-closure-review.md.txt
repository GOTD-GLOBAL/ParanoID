# Bounded closure review — DEPLOY-GATE-1 diagnostic and ORCH-1/2 corrections

## Verdict: **PASS** (bounded closure only)

All three closure subjects are met. **DEPLOY-GATE-1's diagnostic condition: CLOSED. ORCH-1: CLOSED. ORCH-2: CLOSED.** No blocker remains for the parent to create the owner-private gate and proceed with the attended existing-service `update-v2`, subject to the already-standing sequence conditions listed at the end. This verdict is a scoped gate closure — it is not ADR acceptance, not a claim the original failure is fixed, and not evidence that any deployment has occurred (none has).

## Exact reviewed revision

- Product (unchanged, not rebuilt): source manifest `0f6d0689…c314f2e` (254 files), APK `1b9f44d8…e42eadb`, server release `3ed25173ad978e6b417c` (tar `2306c0a8…4630f0`, binary `bf74a963…22fa2`, controller `c706b5dc…298f9e`). `pre-closure-source-artifact-verification.json` records the coordinator's actual byte recomputation of all 254 files in both frozen/integration trees plus APK and tar, all matching (2026-09-09T20:48:26Z).
- Wrapper (new revision under review): `scripts-sha256.json` listing `remote.py` `65be200a…da875`, `local.py` `7daf805b…7b558c`, `contract.json` `a9b7da9e…168016` (unchanged from reviewed-v1). The manifest-file hash `2a30e206…c4133b` is recorded consistently in both the actual parser-execution record and the gate template. **Limitation:** I have read-only file tools and no shell, so I verified hash consistency across independent evidence records, not by recomputing digests myself.

## Subject 1 — DEPLOY-GATE-1 diagnostic condition: CLOSED

The main review required exactly one of: (a) one full original packaged `test_v2_update.py` run with the `run` child's stderr captured plus `postmaster.pid` facts and PG-start elapsed time, or (b) explicit owner risk acceptance. The coordinator took route (a); no owner risk acceptance was supplied, needed, or asserted.

What I verified:

- **Exactly one full-11 run, original order, actual PASS.** `full11.log` shows the same 11 tests in the original alphabetical order, including the originally failing `test_same_schema_binary_restarts_with_original_pinned_tls`, `Ran 11 tests in 48.206s / OK`, and `Gate1-diagnostic-result.json` records 11/11, exit 0, `exactly_one_requested_full11_run: true`.
- **No assertion weakening.** I read all three instrumentation files. `gate1_test_driver.py` wraps `wait_health` and `backup_v2` around the originals, calls the original first, re-raises any failure unchanged, and touches no retry count, timeout, or assertion; `gate1_observer.py` only redirects child stderr from the test's original DEVNULL to retained 0600 files and records monotonic timestamps/PID facts; `gate1_controller_driver.py` runs the exact packaged controller via `runpy` with the same observation hooks. The stderr redirection is precisely the capture the gate requested, not a behavior change.
- **The requested facts were captured.** PG `Popen` to first successful `SELECT 1`: 73.674 ms (candidate `3ed25173…`) and 75.619 ms (retained old `346f0599…`); outer `wait_health` elapsed 582.152 ms and 572.638 ms — all far inside the 10 s/20 s budgets the main review identified as plausible contributors. `postmaster.pid` was absent before start, present and matching the observer-owned PG PID at readiness, and absent after shutdown; both PG exits were 0; outer/Rust stderr empty, PG stderr retained. Before/after 254-file hash records and the final APK/tar checks confirm nothing in the product was modified.
- **The original failure is preserved, not relabeled.** `final-packaged-deploy-tests/HANDOFF.md` and `result.json` still record 13 PASS/1 FAIL with root cause UNKNOWN. The diagnostic result itself states `initial_failure_root_cause: UNKNOWN` and that no fix is claimed. I concur and preserve that: this diagnostic satisfies the requested observation condition; it does not prove the failure will never recur. Because the failure mode is fail-closed (child exit 1, tested same-current-data rollback, worst case a stopped unit with data retained) it is acceptable **only for attended** deployment; unattended execution remains excluded.

## Subject 2 — ORCH-1 and ORCH-2: CLOSED, minimal scope, actual TDD

- **Red is genuine.** `remote-runner-orch12-red.log` shows exactly the two new tests failing against the pre-fix code: the stored record held 133,120 bytes (> 131,072), and `BlockingIOError` was not raised with a third-party lock holder.
- **Green is genuine.** `remote-runner-orch12-green.log` shows 22/22 PASS — the original 20 reviewed tests plus the two new ones.
- **ORCH-1** (`local.py:118-133`): remote stdout/stderr are now truncated to the combined 128 KiB bound *before* `record.update`, with a loop that also accounts for JSON-escaping overhead in the persisted form; the record gains `diagnostics_original_bytes`/`diagnostics_truncated`, the state becomes `ssh_output_limit_exceeded`, and the exit code is forced to 1 on truncation. This matches the scripts review's minimal fix exactly. Consistent with the stated non-claim: `subprocess.run(capture_output=True)` still buffers the full remote output in wrapper memory transiently before truncation — the persisted-record bloat is fixed; no streaming/bounded-memory property is claimed, and I flag none as required.
- **ORCH-2** (`remote.py:370-371`): the worker now executes `fcntl.flock(lock_fd, LOCK_EX | LOCK_NB)` directly on the inherited descriptor before the original probe — a no-op when this open file description already holds the lock, and `BlockingIOError` when a third party holds it. The raise occurs before any status mutation or controller invocation; the new test confirms refusal with the controller call count at zero. Exactly the recommended fix.
- **No broadened scope.** I read the current `local.py` and `remote.py` in full and compared them line-by-line against `remote-update-runner-reviewed-v1/`: the only differences are the two patch hunks; `test_local.py` changes are the two new tests plus a `contextlib` import; `contract.json` is byte-pinned unchanged and its content still binds the exact host/root/unit/PG-system-ID/release/neighbor scope. Directory membership is unchanged from v1 (execution files, tests, README, unapproved gate template). I found no new blocking issue in the changed code; the only observation is cosmetic — the truncation loop halves by characters while measuring bytes, so a truncated record may persist somewhat less than the full 128 KiB allowance, which is fail-safe.

## Additional bounded evidence inspected

- `remote-runner-actual-backup-parser.json` + `verify-real-backup-manifest-parser.py`: the frozen `backup_manifests` parser accepted the actual final-controller-generated, authenticated-restore-verified aggregate manifest from the one Gate-1 run (sample SHA `4785a994…840f81`, generated by controller `c706b5dc…`) and rejected all four negative variants (verified=false, wrong PG ID, non-aggregate row digest, multiple new backups). The script reads only the 13 aggregate/public manifest fields — no key, ciphertext, raw rows, or TLS material.
- `retained-old-capability-check.json`: local execution of the byte-identical retained old binary `3418332f…` returned exactly the capability tuple (`deployment_api: 1`, `runtime: self-service-v2-exact-ip-v1`, schema `ff401a46…`) that wrapper preflight requires, closing the scripts review's capability-expectation concern locally. The **live** host binary's response remains NOT RUN; a mismatch there refuses safely before any mutation.
- `fable-final-retry-process.json` and `fable-scripts-process.json`: both prior reviews are actual `claude-fable-5` successes (`subtype: success`, `is_error: false`, nonzero fable output tokens, distinct sessions), matching the gate's two-distinct-results requirement.
- `remote-runner-orch12-unapproved-gate.log`: the unapproved gate template still refuses (`REFUSED … ValueError`), and `gate-template.json` remains `approved: false` with placeholder result hashes — the manual gate genuinely does not exist yet.
- Authority: `overnight-implementation.md` and `owner-clarifications.md` authorize exactly this attended existing-service update after independent Fable review with blockers resolved; both prior reviews expressly permit it once their listed conditions are met. The conditions are now met.

## Residual limitations and standing conditions (unchanged, not blockers of this closure)

1. The original intermittent readiness failure's root cause remains **UNKNOWN**; it is an availability-only, fail-closed risk acceptable for **attended** deployment with the reviewed same-data rollback. Do not run this unattended, and do not represent the failure as fixed anywhere downstream.
2. ORCH-3 remains a deliberate manual control: the parent must read both full review texts (a mechanically "successful" result file could still contain a blocking text) and set `all_blockers_resolved` by their own judgment when creating the 0600 gate binding the main result, this closure result, the original script report, script manifest `2a30e206…`, and documented item dispositions.
3. Permitted action order stays exactly: `check-gate` → `stage` → `start` → poll `status` to `complete` → `postflight` with the verified-backup record, then two-synthetic-peer hosted acceptance via the reviewed pinned-TLS path. SSH exit is never completion; no failed/unconfirmed state is rerun without fresh owner inspection; never any destructive/fresh/replace/stale-dump/neighbor/credential/TLS/firewall/DNS/publication/merge/phone action.
4. Known fail-closed brittleness stands as previously documented: frozen neighbor PIDs/timestamps, no wrapper timeout on the controller's systemd calls, partial-stage residue requiring manual inspection.
5. I am an AI reviewer (claude-fable-5) with read-only tools and no shell: hash agreement was verified across independent records, not recomputed; no live host, physical-device, or production evidence exists; nothing here is a human audit, permanent ADR acceptance, or proof of deployment.
