## Reviewer identity and evidence basis

Fresh independent review. Model ID as reported by this runtime: `claude-opus-5` — record the actual `modelUsage`, not a requested label. Read-only: two targeted reads (`deploy/turn/paranoid-turn.service.in`, `test_credential_matrix.py`) plus the collated frozen inputs. I executed nothing and computed no digest. Every hash, exit code and log below is **supplied recorded evidence**; "verified" means I compared supplied values for mutual consistency.

## A. ABI and reader exactness — confirmed

The fixed 44-byte layout is arithmetically exact: 4-byte version + 5 × 8-byte (`<HHI`) entries. Tags match the Linux `posix_acl_xattr` constants in the kernel's stored order — USER_OBJ 1, USER 2, GROUP_OBJ 4, MASK 16, OTHER 32 — with `0xffffffff` for every non-named ID. Directory `(5,5,0,5,0)` and file `(4,4,0,4,0)` reproduce the measured observation exactly; with GROUP_OBJ 0 and MASK equal to the named grant, the named UID's effective permission is unchanged and no owning group or other identity gains access, which is why accepting a nonzero owning GID is correct rather than lax. `_read_acl` performs one typed `fgetxattr` with explicit `c_int/c_char_p/c_void_p/c_size_t → c_ssize_t`, rejects a missing symbol and any length ≠ 44 — no discovery loop, no library search, no alternative parser. Layout is selected from `fstat` uid/mode *before* any ACL syscall; the fallback branch provably makes no ACL call.

The runtime matrix exercised **both** branches against real delivery: system cases report directory mode 360 (0o550) / file 288 (0o440) with `acl_size 44`, `entry_count 5`, `acl_errno 0`; user cases report 320 (0o500) / 256 (0o400) with `acl_size -1`, `acl_errno 61` (ENODATA). That is the two-branch contract confirmed by observation, not assumption.

**New correspondence check (my read).** `paranoid-turn.service.in` uses `LoadCredential=voice-turn-secret:/etc/paranoid-turn/relay.secret` and `RuntimeDirectory=paranoid-turn`, which yield exactly the two literals hard-coded in `read_systemd_secret()` (`/run/credentials/paranoid-turn.service`, `/run/paranoid-turn`) and the exact credential filename `voice-turn-secret` opened relative to the held dirfd. Unit ↔ reader agree; no third path exists.

**The NOT RUN claim on the production entry point is structurally forced, not merely asserted.** The matrix used transient unit names and `User=codex`, so `CREDENTIALS_DIRECTORY` could never equal the literal and `read_systemd_secret()` would have refused. `production_entry_point: NOT_RUN` is therefore correct and unavoidable — the helper exercised the four copied primitives, not the gate. `User=paranoid-turn` is a name, so persistent-unit/static-UID confirmation likewise stays open.

## B. Closure of the prior conditional — verified as implemented

A1: `EntryPointTests` asserts both `check` and `run` reach the no-argument strict reader before config/exec, with the generic `read_secret` wired to fail; suite is 22/22 OK (`review-closure-offline-0.log`, exit 0). B1: `test_generated_helper_executes_real_private_fallback_and_format_refusal` actually spawns the generated helper in a real subprocess over two unprivileged fixtures and asserts the exact accepted envelope, the exact `credential_primitive/format` rejection, exit 2 and empty stderr — the one-shot risk I would otherwise have flagged is retired. B2/B3/B5 are visible in the result: `output` and `properties` persisted per case, `credential_dir_exists: true` recorded before validation, closed bounded numeric metadata with schema negatives tested. B4: `production_relevance` is explicit per case (`system_descriptor_primitive` vs `informational_user_manager_only`). Runtime source is unchanged at `6122051f…` across freeze, regression report, matrix result and package manifest.

## C. Artifact ↔ source correspondence — exact

- `manifest.json` `runtime.py` = `6122051f…` = frozen source = `credential-matrix-freeze.json` = `result.json`.
- `manifest.json` `turnserver.conf.in` = `a328a79f39e76569ee0ad7499b0f189880ab5c09ea03428b736d83600e46712e` = the `TEMPLATE_SHA256` constant compiled into the packaged `runtime.py`. Template pin and packaged template are the same bytes; `render_config` cannot admit an unreviewed profile in this artifact.
- `README.md` package member = frozen README; `package.py` member = frozen source.
- Guard contract's historical `runtime.py` `e302d7cc…` equals `changed_members.old` in the artifact record — the three changed members (README, binary, runtime) are the complete delta, and `alpha.py`, `voice_turn.rs`, `single_host*.py` are byte-identical.
- Manifest membership matches `build.py`'s fixed copy list exactly; no test file, secret or stray member entered the package.

Native binary: `.text`/`.data` identical, differences confined to `.note.gnu.build-id` and three debug sections, with `.debug_line_str` equal after build-directory normalization (size delta 6 bytes) and `.debug_info`/`.debug_line` same-size/different-content — the expected consequence of shifted string offsets. That is credible evidence of functional identity and correctly carries **no** runtime acceptance; no prior receipt is reused.

## D. Concrete findings (none blocking)

**F1 — evidence-index ambiguity (fix before final review).** `credential-regression-report.json` pins `deploy/turn/test_systemd_credentials.py = 616e0c34…`; the current file is `98b40d21…` (22 tests). The regression report predates A1, and `runtime.py` is identical in both, so nothing is false — but the frozen set now carries two hashes for one path with no stated ordering. Add one line: regression report = pre-A1 21-test run; `review-closure-offline-0.log` = current 22-test run.

**F2 — stale doc statement.** `voice-single-host.md` ("Current production refusal and reproducibility limits") still says the native hash difference's "cause is unmeasured." `turn-native-section-comparison.json` measures it for this pair. Narrow or update that sentence; do not upgrade it into a reproducibility claim.

**F3 — CHANGELOG understates.** The 2026-09-10 entry says "actual unit delivery … remain separate pending gates." Six cases delivered credentials through actual transient system and user units. Narrow to *persistent production unit / static UID delivery*, which is genuinely pending.

**F4 — matrix guard is outside CI.** `run_credential_matrix.py` / `test_credential_matrix.py` live in the evidence tree, so the exact-AST guard binding the helper to the four production primitives never runs in `server.yml`. A future `runtime.py` edit would silently invalidate the frozen matrix with no failing test. Either land the offline parts under `deploy/turn`, or state explicitly that this evidence is bound to `6122051f…` and expires with it. Repo-side CI wiring is otherwise correct: `deploy/**` triggers, and `test_systemd_credentials.py` is in the `native-package` job.

**F5 — provenance nuance.** Manifest records `source_dirty: true`; `source_commit 4f823340…` therefore does not identify the source. Correspondence rests on member hashes, which do match — cite it that way, never as commit-level provenance.

**F6 — `deployment_authority` field.** Replacing `NOT AUTHORIZED` with `NOT RUN` plus the explicit conditional string is accurate against REQ-DEPLOY-003 and does not weaken anything, since `live_deployment: NOT RUN` remains. It is a provenance note, not authorization, and must never be cited as the separate bounded deployment approval. `README.md`'s sentence about the retained manifest's historical `NOT AUTHORIZED` field now describes only the older archives; keep it scoped that way.

## Verdict: **APPROVED** — credential-only source and artifact, this scope only

No blocking security or correctness finding. F1 and F3 are record corrections; F2/F4/F5 are follow-ups; none alters code or artifact bytes, and I recommend landing them before the final review rather than treating them as gates here.

This approves reviewed source and the `948cec15b9f186a7ddbf` package (`0eab990b…`) as corresponding to it. It approves nothing else. Unchanged and **NOT RUN**: TURN-RT01 expiry/race/drain, TURN-ACL02 and effective own-host port denial, CLI/5766 closure, coturn relay lifecycle and packet policy, production entry point, persistent production unit/static UID, full coordinator, CALL-CURRENT01, hosted installation, deployment. **CALL-CONNECT01 stays OPEN.** No relay acceptance is inferable from the primitive matrix, the ELF comparison, or this approval. The missing full-relay fixture profile means production apply remains fail-closed and the mandatory production gate structurally unsatisfiable in this candidate. ADR-0012 stays proposed; final independent review and bounded owner deployment authorization remain separate and unmet.