# Candidate profile/verifier review closure — 2026-09-10

Actual independent claude-opus-5 returned CONDITIONAL with ten corrections. The
full fixture runner remains unimplemented and availability remains EMPTY; neither
this closure nor any parser fixture is runtime acceptance.

1. Boundary isolation hash is computed from the current normalized kernel
   observation and compared with the manifest, not accepted as an arbitrary hash.
2. Report embeds BOTH fresh and retained-v8 fixture intents and recomputed plans;
   IP, complete message and relay identity/layout blocks equal production.
   Deliberate synthetic exceptions are profile/interface/kit, mode-specific
   expected identity and TLS/data hashes. Comparing fixture TLS/database identity
   to production would require copying production private keys/data, which the
   authorized isolation expressly forbids; no such copying occurs. Mode coverage
   is verified, not inferred. Actual read-only host facts confirm UID1003/GID1004,
   enp5s0 and available relay UID/GID1902 for the eventual fixture intent.
3. Five existing production gate hashes must equal the corresponding verified
   case-log hashes, so the same evidence backs both gate entries and rehearsal.
4. Added explicit fixture-isolation case keys for pre-address inventory, final
   loopback/no-route cleanup, exact message ingress and UFW loopback-accept order.
5. The hash-only acceptance test opens availability ONLY under an offline patch,
   and asserts the rejection is not FullRehearsalUnavailable.
6. Availability is constrained to a subset of the VM profile; the production kit
   rejects runner membership. Actual constant remains empty.
7. Recording ownership stubs assert owner0 evidence reads and root-kit verification;
   an actual unprivileged read with owner0 is rejected. Root ownership remains
   explicitly simulated for positive parser fixtures, not runtime attestation.
8. Kernel observations use bounded nofollow/nonblocking same-fd reads; boundary
   mode0400 is checked on that fd. Physical NICs and extra namespaces are refused.
   Additional genuine RED/GREEN verifies reciprocal actual veth peer indices and
   kind, preventing mere renamed-interface assertions. Source copies are root-owned
   and resolved before reading. The inherited kernel/proc observations concern new
   guest/self-owned processes only, not any previously refused Android/host trace.
9. Production interface names require an initial alphanumeric character.
10. Root requirement now precedes plan/kit/module loading for both full profiles.

Nineteen final new offline tests pass. All prior RED, first GREEN fixture failure,
ten-test GREEN, correction attempt with two fixture bugs,18-test GREEN and veth
pair RED/GREEN are preserved. The correction attempt exposed shared synthetic
intent dictionaries and plan invalidation when test availability changed; those
test harness errors were corrected without changing production validation.
Availability changes alter plan hashes. Receipts have no clock expiry, and retain
artifact/plan binding plus mandatory independent actual runtime review.

The initial reviewer described a fifth state_record boundary call; actual code
guards preflight, apply, status and rollback, while state_record validates profile
and state path. Extraction is root-owned artifact staging, not fixture execution.
No code or result is promoted by that imprecise count. Final exact runner/artifact
and runtime review still must cover the complete implementation before exposure.
