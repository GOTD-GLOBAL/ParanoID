# Candidate examples, not host inventory or acceptance

The example pins the actual final kit manifest. REPLACE_IFACE, numeric IDs 20001/20002,
all-zero expected hashes/release and PG identifier 0 are obvious input placeholders;
replace them with exact read-only host observations and operator-selected unused IDs.
Do not use these identity values as deployment authority. This is the fixed existing
host layout. Plan output demonstrates the complete interface without contacting it.

actual-plan.json is actual CLI output. actual-refusal.json records actual exit 2,
full-relay-rehearsal-unavailable and mutations:false. The acceptance file deliberately
contains no PASS assertions. This candidate cannot activate a production relay.

Use the extracted candidate README command interface with exact full absolute paths,
kit digest and reviewed plan hash. PostgreSQL16 is a preinstalled prerequisite; the
coordinator controls only its private cluster. No component SSH recipe is required.
