## Verdict: APPROVE — publish/commit the test-only changes and sanitized bounded evidence to draft PR20 / issue 19. Independent CALL-CONNECT01 disposition: **OPEN** (causal closure insufficient).

### What I verified

**Claims match the raw evidence exactly.** Every number in the README, `ordinary-summary.json`, `current-state.md` and CHANGELOG traces to the full timeline in `ordinary-comparison-result.json`: baseline SDK safe error `connection` at 16496.318 ms after accepted Answer with 23357 ms setup / 13541 ms silence budget remaining (ruling out setup expiry as the observed terminal path), `mediaState` termination 0.182 ms later, zero publication callbacks and zero host committed-answer/setRemote events; fixed arm publication at 762.449 ms, one committed-answer IPC receipt and one setRemote-answer success, connected callback at 903.942 ms, relay/relay DTLS-connected Opus with matching sample counts and energies on both sides (Android 251040 samples / energy 0.0874; host 259 frames / 248640 samples), then normal hangup (`received`/`hangup`) and complete cleanup on both sides. The host baseline events show no host SDK failure preceding Android's end, exactly as the README states.

**No overclaiming.** The fixed arm is classified `NOT_REPRODUCED`, never "PASS" or causal proof; the summary's `limitations` list and the README explicitly preserve every critical unknown the task enumerates: no source-time native ICE cause or pre-cleanup SDK state (`partial_no_sdk_precleanup_hook` retained end-to-end), no complete auth/lifecycle stream, no historical first-cause trace, sequential arms with differing pre-Answer UI timing and permission state, failed `run-as` proc reads honestly recorded as `NOT_MEASURED` with `same_process_association: false` framed as unverified start ticks (not detected replacement), host initial `starting` sanitized to `unknown` without retrospective reconstruction, and host PCM energy 5.11e-10 described as near-silent actual microphone input, not a synthetic reverse tone.

**Provenance chain is closed.** The driver hash embedded in the result (`9ce2a1b2…`) equals the hash in `ordinary-driver-command.json`; the retained `run_ordinary_comparison.py.txt` matches the runtime source (head and tail compared); host observer, Android observer, and sampler hashes in the result match `observer-extended-manifest.json`; per-arm engine hashes match `independent-source-observer-verification.json`, which independently hashed all 59 copied product inputs and both observer DEXes (identical) rather than trusting claim files. Installed APK bytes were read back and hashed before/after each arm with equal PIDs. Setup correction (1, PostgreSQL socket layout, pre-arm), preservation record, and driver exit 0 are all internally consistent; no rerun exists anywhere in the record.

**Observer transparency and privacy hold.** I grepped the entire 285 KB timeline: no SDP, addresses, fingerprints, contacts, accounts, secrets, or PCM — only enum states, bounded counters, budgets, and command action names/exit codes. The runtime driver restricts itself to the allowlisted observer commands, keeps UI XML and pairing contact material memory-only, and `ordinary_host_peer.py` discards the raw SDP/stat write sites while delegating all product behavior unchanged (exceptions re-raised unstringified, observation fenced so its failure cannot alter the call). The current sampler enforces the documented invariants (controller sample before any media query at `call_connect_sampler.py:127`, no queries after ended/censor, censoring never fabricates a terminal).

### Why CALL-CONNECT01 stays OPEN

One successful fixed arm is a single non-reproduction under sequential, not-fully-controlled conditions (differing pre-Answer timing/permission state; prolonged gathering duration uncontrolled). The baseline arm narrows the observed terminal path (aggregate PeerConnection FAILED delivery, budgets intact, no publication) but the native first cause was structurally unmeasurable with this observer, and the historical failure has no retained trace. The documentation itself draws this line correctly; keeping the finding OPEN is the only disposition the evidence supports.

### Non-blocking correctable nits (documentation only)

1. `product_source_manifest_sha256` is identical across both arms because `build_ordinary_fixture.py` hashes the manifest from the worktree *before* substituting the baseline engine (and the fixture KeyClient realm/pin). A one-line clarification in the README would prevent a reader from mistaking it for a per-arm copied-tree attestation; the independent verification already supersedes it.
2. `CALL-CONNECT01.md:209` cites `safe-extension-red.log and safe-extension-green.log`; the committed files are `safe-extension-closing-red.log` and `safe-extension-final-green.log`. Align the names.

Neither affects the verdict. No production-source seam, rerun, TURN/security work, or deploy/merge is requested or implied by this approval; acceptance, deployment, and merge remain blocked as documented.
