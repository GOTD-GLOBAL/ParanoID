# PR54 merge preparation review — 2026-09-22

Independent reviewer: Claude Opus 5 in fresh contexts. Base `60799d8`, original
head `35f50bf`, plus the CI/documentation closure diff. No runtime edits after
the source review. Initial tool-assisted review hit a turn limit and its resume
was unavailable; neither was counted as approval. Fresh complete review and
closure follow below. G1 (durable owner scope approval) remains OPEN: source
merge is blocked, not implicitly approved by this report.

## Coordinator execution evidence

- Rust format/clippy, eight native tests and real JNI/controller subset PASS.
- Full `check_host.py` with the previously built pinned SBF PASS, including
  nineteen program-pin negatives; no fresh chain transaction or deployment.
- Actual Android35 SDK compilation and call/controller/log, UI, background,
  updater/session-worker/artifact and crash-boundary regressions PASS.
- CI wiring regression RED before adding the job/path triggers, GREEN after.
  Cold-cache preparation regression RED before core fetch, GREEN after.
- An isolated temporary CARGO_HOME with only Devnet dependencies fetched really
  failed core offline metadata (missing uuid); fetching the locked core closure
  made offline metadata and notices pass. No inherited Cargo cache assumption.
- Nine Devnet receipt-validator tests PASS. Markdown CLI2 0.18.1 (CI version)
  passes, as do changed-document local links and git whitespace checks. An initial
  unpinned newer linter reported unrelated existing table-style warnings; canonical
  version used without weakening rules. No closed metadata-key schema was found
  in scripts; actual docs CI remains the final gate.
- New runner is staged; no server/core/key-protocol or production Java/Rust
  modifications are part of this follow-up. No signed APK rebuild, publication,
  phone action, DB reset or host operation. Original artifact evidence is retained.

## Initial completed review

### PR54 merge review — independent, read-only

**Scope reviewed:** `fix/pr54-closure` @ `35f50bf` vs `origin/main` @ `60799d8` — 48 files, +1677/−121, **including uncommitted work**.

**Uncommitted diff scope (5 modified, 1 untracked):**
`.github/workflows/server.yml`, `CHANGELOG.md`, `docs/project/current-state.md`, `docs/project/evidence/android-v28-devnet-20260922/README.md`, `scripts/test-ci-wiring.py`, and untracked `clients/android-devnet/check_controller.py`. The entire PR54 CI-closure deliverable is in the working tree, not in any commit.

---

## Blockers

**B1 — `check_controller.py` is untracked; the new CI job would fail on a clean checkout.**
`.github/workflows/server.yml:144` runs `python3 clients/android-devnet/check_controller.py`, but `git status` shows the file as `??`. Merging the committed tree gives `devnet-client` a missing file. Worse, `scripts/test-ci-wiring.py:16` only string-matches the workflow text, so the new wiring test passes green while the referenced script does not exist — false assurance on exactly the gate PR54 adds. Fix: stage the file; ideally have the wiring test assert existence of each referenced script path.

**B2 — the `devnet-client` job cannot pass `test_devnet_notices.py` on a cold runner.**
That test shells out to `clients/android/notices.py`, which now iterates *two* manifests (`notices.py:6`) and calls `cargo metadata --offline --locked --manifest-path ../core/Cargo.toml`. The job (`server.yml:136-143`) only ever runs cargo against `blockchain/solana/client`, so `clients/core`'s registry deps (vodozemac `=0.10.0`, `jni`, `uuid`, `sha2`, …) are never fetched or extracted — `--offline` errors out. Separate crates, separate lockfiles, no shared workspace, and `dependencies.py` fetches Maven jars only. This passes on the build host solely because `build.sh` compiles `clients/core` first and warms `~/.cargo` (I confirmed `vodozemac-0.10.0` is extracted in the local registry). Fix: add `cargo +1.98.1 fetch --locked --manifest-path clients/core/Cargo.toml` before the notices step, or drop `--offline` from the metadata call.

**G1 — procedural gate: no durable approval record for this scope or for the merge.**
`docs/governance/documentation-policy.md` ("Durable approval and delegation records") accepts either a GitHub review/comment by the approver, or a Telegram message with permalink + message ID + sender mapping + timestamp + exact text. Neither exists here. RFC-0026:42-45 records the instruction as «Го. Делай мне APK.» with "no permalink is available here", and states outright that **"permanent owner confirmation belongs in the new integration PR"** — PR54 does not contain it. The one durable link (PR53 `issuecomment-5771635821`) is, per RFC-0026:20-22, scope approval for the earlier Devnet slice, not this delivery. Closed-alpha exception item 1 requires "a permanent owner approval for that scope"; the Telegram merge/issue-52 authorization you describe does not yet meet the evidence rule. Note this gate is narrow: ADR-0015 stays `proposed` and RFC-0026 stays `draft`, so no acceptance transition fires and the full acceptance-evidence machinery is not triggered — what is missing is scope/merge authorization in durable form.

## Non-blockers

- **ADR-0015 is absent from `docs/decisions/README.md`.** Every other proposed ADR (0004–0014) is indexed there; this decision is unreachable from the decision log.
- **`last_reviewed` not bumped** despite new 2026-09-22 sections: `current-state.md` still `2026-09-18`, `threat-model.md` still `2026-09-09`. Flagged in `review-closure.json` as "close before merge"; still open.
- **Run-together numerals persist** in new prose: "the24 words", "version28", "via24 words", "and19 program-pin", "funding0.01", "Android35", "on2026-09-22" (RFC-0026:22), "reviewed73800-byte" (CHANGELOG). Editorial, but in evidence and normative docs.
- **`RegistrationFlow.expired()` (`:32-34`) never inspects signature statuses** — it validates only array length, then decides purely on block height. The outcome is safe because finalized PDA readback runs first and is authoritative, but RFC-0026:172-175 describes inspecting finalized success/error; the RPC call is dead weight as written.
- **`DevnetWork.run` fallback text** (`:20`) tells the user to "нажмите проверку ника" for failures that never sent a transaction (`identity_required`, `cost_limit`, faucet `rpc_unavailable`), and it dropped the old `[a-z_]`-safe error-code surfacing, leaving only "(IOException)". Flagged in the prior closure; still open.
- **`fund()` persists `faucet_attempted=true` before the airdrop RPC**, so any network error burns the single allowed attempt permanently. Fail-closed by design, but the only remaining path is the copy-address fallback.
- **`Command::Identity` derives the identity PDA via `addresses(&public, "aaa").0`** (`lib.rs:238`) — a dummy name that works only because identity derivation ignores the name. Correct today, silently wrong if seeds change.
- **`check_host.py` (full gate, incl. `RpcProgramTest` + exact SBF pin) is still not wired into `build.sh` or CI.** `check_controller.py` documents itself as a subset that does not replace it; that evidence stays manual.

## Code merge verdict

**The touched Java and Rust are sound — no security or correctness blocker found.** Specifically verified: `prepare` is genuinely unsigned and the `prepared.message == tx.message` equality check (`RegistrationFlow.java:87`) binds the fee/rent estimate to the exact bytes later signed; cost and balance gates precede signing; retries rebroadcast identical wire bytes with a real 3-submission bound persisted *before* each send (`:39`); the attempts ledger is pruned only after finalized height exceeds every recorded `last_valid_height` *and* a re-read (`:74-77`), removing v27's lifetime `attempt_limit` dead end; `DevnetStore` fails closed on a leftover write-intent marker and never rekeys or resets; `LinkageError` containment keeps a missing native lib from killing the messenger; the active-call guard in `openDevnet()` is real; record verification in Rust compares all 128 bytes including zero padding and canonical bump, with base64 re-encoding checks rejecting non-canonical input; `deny_unknown_fields` plus the duplicate-key test close the request-shaping surface. The APK gates correctly assert exactly three native libs and the new dex classes.

**Verdict: code APPROVE; merge BLOCKED on B1, B2 and G1.** B1 and B2 are mechanical and land in the same working tree you already have open. G1 is yours and the owner's to clear — the repo's own text names PR54 as where that confirmation belongs, so a GitHub comment from `martadvix-web` on PR54 stating the approved scope would close it. I have not applied any fix, run any deploy, or touched chain/server state.

Two caveats on my own evidence: B2 is established by reading the job definition and confirming the local cache explains the host-side pass — the decisive `CARGO_HOME`-isolated `cargo metadata` run was denied by the sandbox, so it is reasoned, not executed. And the prior private-handoff Fable APPROVE in `review-closure.json` is scoped `"private artifact handoff only; not feed/merge/production"`; I treated it as input, not as merge review, and independently re-derived the findings above.

## Closure review

### PR54 closure re-review — mechanical corrections only

**Verdict: APPROVE for code/CI closure. Merge remains BLOCKED on G1 alone.**

Reviewed the diff packet as given, read-only, no tools. Scope limited to the closure changes; the unmodified Java/Rust runtime keeps the prior APPROVE and is not re-derived here.

---

## B1 — CLOSED

`clients/android-devnet/check_controller.py` now shows staged-add status (`A` in the index column), so it lands in the commit and `devnet-client` resolves on a clean checkout.

The stronger part of the fix is the wiring test: `if command.startswith('python3 '): self.assertTrue((ROOT / command.split()[1]).is_file())` now asserts existence for all three referenced scripts. That converts the original false-green into a real gate — CI checks out the committed tree, so an unstaged script fails `test-ci-wiring.py` RED rather than passing the wiring test and dying later in the job. The `split()[1]` extraction is taken from the test's own command string, so the `JSON_JAR=` prefix on the workflow line doesn't break it. The job-slice asserts also fail loudly (IndexError) if the job is renamed or reordered rather than silently matching nothing.

Path triggers are correct and necessary: `blockchain/**` was previously not a trigger path at all, so the new job would never have fired on the code it gates. Count-of-2 assertion covers both `pull_request` and `push`.

## B2 — CLOSED

`cargo +1.98.1 fetch --locked --manifest-path clients/core/Cargo.toml` sits before `test_devnet_notices.py`, and the wiring test pins that ordering by index rather than just presence. `blockchain/solana/client`'s tree is warmed by the earlier `test`/`build --locked`, so both manifests `notices.py` walks are populated before the offline `cargo metadata` runs.

The isolated-`CARGO_HOME` reproduction upgrades this from reasoned to executed: empty cache → metadata failure, both lockfiles fetched → offline metadata and notices pass. That is exactly the run the sandbox denied me last round, and it confirms `fetch` is sufficient (extraction from the cached `.crate` needs no network). `assertNotIn('continue-on-error:', job)` keeps the new gate blocking.

## Non-blockers confirmed fixed

- **ADR-0015 indexed** in `docs/decisions/README.md`, with the correct disclaimer that source merge does not accept the ADR.
- **`last_reviewed` bumped** on `current-state.md` and `threat-model.md`, and the added `last_reviewed_scope` key is the right correction — it records *what* was reviewed instead of implying a blanket renewed audit of two documents whose bodies barely changed.

## G1 — OPEN, still blocks merge

Nothing in this diff creates a durable approval record, and I want to be precise about a new hazard it introduces: `current-state.md` now states in prose that the owner "requests completion of PR51/PR54 and issue52," and the evidence README records the Telegram handoff and the registration report. Both are appropriately framed as owner *reports*, and neither claims policy compliance — that framing is correct. But these lines must not be cited as satisfying `documentation-policy.md`; a self-authored paragraph describing an instruction is precisely what the permalink + message ID + sender + timestamp rule exists to exclude. G1 closes only with a GitHub review/comment from the approver on PR54, or a conforming Telegram record. ADR-0015 stays `proposed`, RFC-0026 stays `draft`, so no acceptance transition fires.

## Watch items (none blocking)

1. **Verify the front-matter schema accepts `last_reviewed_scope`.** This is a new key in `status/owner/last_reviewed` front matter. If the repo's docs-metadata check enforces a closed key set, this fails CI. Worth one local run of that gate before pushing — it's the only thing in this diff I'd want confirmed by execution.
2. **`timeout-minutes: 15` on a cold `devnet-client`.** fmt + clippy `--all-targets` + test + build is roughly two full compiles of the client crate's tree from an unwarmed runner cache. If that crate pulls the full Solana SDK tree, 15 minutes is tight on a 2-core runner. Failure mode is loud, not silent, so it's a flakiness risk rather than a correctness one — consider 30.
3. **"B1/B2" label collision.** The evidence README says the retained closure "approved private artifact handoff after B1/B2 corrections." If those are the artifact-round items and not this PR's CI blockers, the bare labels invite a future reader to think `review-closure.json` blessed the CI work — it did not, its scope is `"private artifact handoff only; not feed/merge/production"`. Name the items or say "the artifact-scope blockers recorded in that file."
4. **Identifier formatting drift within the same PR.** `current-state.md` writes "ADR0015", "RFC0026", "issue52" while `docs/decisions/README.md` correctly writes "ADR-0015" and "RFC-0026". Same run-together class flagged last round; here it also breaks any `ADR-\d{4}` cross-reference matching.
5. **Optional hardening:** the wiring test pins the fetch ordering but not the *set* of manifests `notices.py` reads. A third manifest later goes red-on-cold-runner only. Having `notices.py` expose its manifest list for the test to cross-check would close that.

Prior runtime non-blockers — `RegistrationFlow.expired()` ignoring signature statuses, `DevnetWork.run` fallback copy and dropped error-code surfacing, `fund()` persisting `faucet_attempted` pre-RPC, `Command::Identity`'s dummy `"aaa"` seed, and `check_host.py` still unwired from `build.sh`/CI — are untouched by this diff and correctly out of scope for a mechanical closure. They remain open.

---

I applied no fixes, ran nothing, and made no execution, phone, deploy, feed or chain-state claim. The registration result in these docs is an owner report, not independently observed instrumentation.
